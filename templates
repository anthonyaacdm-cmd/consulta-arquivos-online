<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Consulta de Arquivos</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light p-4">
  <div class="container">
    <h2 class="text-center mb-4">🔍 Consulta de Arquivos Online</h2>
    <form method="POST" class="text-center mb-4">
      <input type="text" name="url" class="form-control mb-3" placeholder="Digite a URL do site..." required>
      <button type="submit" class="btn btn-primary">Consultar</button>
    </form>

    {% if arquivos %}
    <div class="card p-3 shadow-sm">
      <h5>Arquivos encontrados em: <span class="text-primary">{{ url }}</span></h5>
      <ul class="list-group list-group-flush mt-2">
        {% for arquivo in arquivos %}
        <li class="list-group-item">
          <a href="{{ arquivo }}" target="_blank">{{ arquivo }}</a>
        </li>
        {% endfor %}
      </ul>
    </div>
    {% endif %}
  </div>
</body>
</html>
